# Procfile — used by Render (and Heroku-style platforms) as start command
# This is for the AUTH server deployment (authServer.js).
# For the CHAT server, use a separate Render service with: node server.js
web: node authServer.js
